# restapi

This API should support 
/products
    - GET / POST 

/products/{id}
    - GET / PATCH / DELETE

/orders
    - GET / POST

/orders/{id}
    - GET / DELETE

Only logged users can acess protected rows.